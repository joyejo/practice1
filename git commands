1. git --version to check if the git is proprly installed
2. git init to create new local repository
3. git status to check the status the lcal repository
4. git pull to bring all incremental or differential update from remote repository
5. git commit -m ""  take backup to local repository
6. git push -u origin main to push local files to remote repository or take backup to remote  repository